apiVersion: apps/v1
kind: Deployment

metadata:
  name: calculator-deployment

spec:
  replicas: 4

  selector:
    matchLabels:
      app: calculator

  template:
    metadata:
      labels:
        app: calculator

    spec:
      containers:
        - name: calculator-container
          image: spring-boot-calculator:1.0
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8080